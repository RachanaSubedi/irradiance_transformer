[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "irradiance-transformer"
version = "0.1.0"
description = "Transformer-based spatial GHI imputation pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
